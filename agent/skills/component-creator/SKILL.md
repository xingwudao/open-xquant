---
name: component-creator
description: Route open-xquant component creation requests after checking the registry;
  use when users ask for a new Indicator, Signal, Rule, or PortfolioOptimizer.
---

# component-creator OpenCode Adapter

Canonical instructions are single-sourced in `../component-creator.md`.

Before acting, read `../component-creator.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../component-creator.md` instead.
