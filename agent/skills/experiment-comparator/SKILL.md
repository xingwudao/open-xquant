---
name: experiment-comparator
description: Compare two completed open-xquant experiment runs.
---

# experiment-comparator OpenCode Adapter

Canonical instructions are single-sourced in `../experiment-comparator.md`.

Before acting, read `../experiment-comparator.md` completely and follow it as
this skill's body. This adapter exists only so OpenCode can discover the
canonical skill through `<name>/SKILL.md` on platforms where Git symlinks are
unavailable.

Do not add workflow instructions here; update `../experiment-comparator.md` instead.
