---
name: create-rule
description: Create a new open-xquant Rule with portfolio-state tests and registry
  wiring; use after component-creator confirms no existing rule satisfies the requested
  risk or exit behavior.
---

# create-rule OpenCode Adapter

Canonical instructions are single-sourced in `../create-rule.md`.

Before acting, read `../create-rule.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../create-rule.md` instead.
