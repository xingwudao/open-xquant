---
name: chart-indicator
description: Render open-xquant run charts and indicator overlays; use when users
  ask to visualize price, indicators, signals, or chart artifacts.
---

# chart-indicator OpenCode Adapter

Canonical instructions are single-sourced in `../chart-indicator.md`.

Before acting, read `../chart-indicator.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../chart-indicator.md` instead.
