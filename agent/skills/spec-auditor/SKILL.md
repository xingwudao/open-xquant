---
name: spec-auditor
description: Audit strategy_spec.yaml field provenance before backtests.
---

# spec-auditor OpenCode Adapter

Canonical instructions are single-sourced in `../spec-auditor.md`.

Before acting, read `../spec-auditor.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../spec-auditor.md` instead.
