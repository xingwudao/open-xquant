---
name: backtest-runner
description: Compatibility skill for old prompts that ask to run a backtest; redirect
  to strategy-builder for spec creation and audited backtest workflow.
---

# backtest-runner OpenCode Adapter

Canonical instructions are single-sourced in `../backtest-runner.md`.

Before acting, read `../backtest-runner.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../backtest-runner.md` instead.
