---
name: strategy-builder
description: Build and validate open-xquant strategy_spec.yaml files from user strategy
  ideas; use for new strategy design, spec edits, and audited CLI backtest workflows.
---

# strategy-builder OpenCode Adapter

Canonical instructions are single-sourced in `../strategy-builder.md`.

Before acting, read `../strategy-builder.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../strategy-builder.md` instead.
