---
name: create-indicator
description: Create a new open-xquant Indicator with tests and registry wiring; use
  after component-creator confirms no existing indicator satisfies the request.
---

# create-indicator OpenCode Adapter

Canonical instructions are single-sourced in `../create-indicator.md`.

Before acting, read `../create-indicator.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../create-indicator.md` instead.
