---
name: evaluate-time-series
description: Evaluate time-series factors with hit rate, decay curve, profit/loss
  ratio, cash-period behavior, and tearsheets in open-xquant; use for timing or rotation
  signals.
---

# evaluate-time-series OpenCode Adapter

Canonical instructions are single-sourced in `../evaluate-time-series.md`.

Before acting, read `../evaluate-time-series.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../evaluate-time-series.md` instead.
