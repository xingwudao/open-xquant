---
name: trade-executor
description: Configure open-xquant execution timing, fees, slippage, lot size, fill
  price mode, and broker assumptions; use when users discuss trading costs or order
  execution.
---

# trade-executor OpenCode Adapter

Canonical instructions are single-sourced in `../trade-executor.md`.

Before acting, read `../trade-executor.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../trade-executor.md` instead.
