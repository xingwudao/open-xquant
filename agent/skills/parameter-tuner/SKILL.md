---
name: parameter-tuner
description: Tune open-xquant strategy parameters with grid search, walk-forward validation,
  time-series CV, and overfitting checks; use when users ask to optimize parameters.
---

# parameter-tuner OpenCode Adapter

Canonical instructions are single-sourced in `../parameter-tuner.md`.

Before acting, read `../parameter-tuner.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../parameter-tuner.md` instead.
