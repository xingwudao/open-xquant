---
name: factor-evaluator
description: Route open-xquant factor evaluation tasks to cross-sectional or time-series
  workflows; use when users ask whether a factor predicts returns.
---

# factor-evaluator OpenCode Adapter

Canonical instructions are single-sourced in `../factor-evaluator.md`.

Before acting, read `../factor-evaluator.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../factor-evaluator.md` instead.
