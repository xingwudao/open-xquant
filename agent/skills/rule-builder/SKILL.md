---
name: rule-builder
description: Configure and reason about open-xquant trading rules, exits, risk holds,
  and rebalance limits; use when the user asks for stop loss, take profit, drawdown
  guards, holding limits, or rule components.
---

# rule-builder OpenCode Adapter

Canonical instructions are single-sourced in `../rule-builder.md`.

Before acting, read `../rule-builder.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../rule-builder.md` instead.
