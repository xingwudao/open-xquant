---
name: create-signal
description: Create a new open-xquant Signal with deterministic output-domain tests
  and registry wiring; use after component-creator confirms no existing signal satisfies
  the request.
---

# create-signal OpenCode Adapter

Canonical instructions are single-sourced in `../create-signal.md`.

Before acting, read `../create-signal.md` completely and follow it as this skill's
body. This adapter exists only so OpenCode can discover the canonical skill
through `<name>/SKILL.md` on platforms where Git symlinks are unavailable.

Do not add workflow instructions here; update `../create-signal.md` instead.
